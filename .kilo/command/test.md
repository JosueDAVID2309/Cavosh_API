# Run Cavosh API Tests

```bash
cd cavosh_api_node
node src/test_integration.js
```

Runs 24 integration tests covering all API endpoints: auth, productos, sucursales, carrito, cupones, favoritos, pedidos.
