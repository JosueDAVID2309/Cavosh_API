# Run Cavosh API Development Server

```bash
cd cavosh_api_node
npm run dev
```

Starts the Express server with nodemon hot-reload on `http://localhost:8080`.
The app auto-connects to MySQL, syncs models, and seeds data on startup.
